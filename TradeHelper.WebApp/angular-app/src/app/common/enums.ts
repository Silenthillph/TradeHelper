export namespace Enums {
    export enum PositionType {
        Long = 1,
        Short = 2
    }
    export enum PositionStatus {
        Open = 1,
        Closed = 2
    }

    export enum CrudOperation {
        None = 0,
        Create = 1,
        Update = 2,
        Delete = 3
    }
}
